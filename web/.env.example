# Adres backendu Node w dev. Vite proxyuje /api -> tu.
# Backend rozmawia z ESP32 (adres ESP wpisujesz w server/.env, nie tutaj).
VITE_BACKEND_URL=http://localhost:4000

# Dla builda produkcyjnego, gdy frontend serwuje ktos inny niz backend:
# VITE_API_BASE=http://192.168.1.60:4000
